# Test Environment Configuration

# Database
NODE_ENV=test
MONGODB_URL=mongodb://localhost:27017/ai-cv-evaluator

# JWT
JWT_SECRET=test-jwt-secret-key
JWT_ACCESS_EXPIRATION_MINUTES=30
JWT_REFRESH_EXPIRATION_DAYS=30

# LLM Configuration (mocked in tests)
LLM_API_URL=https://openrouter.ai/api/v1
LLM_MODEL=meta-llama/llama-3.1-8b-instruct:free
LLM_TEMPERATURE=0.3
LLM_MAX_TOKENS=2000

# ChromaDB (mocked in tests)
CHROMA_HOST=localhost
CHROMA_PORT=8000

# Redis (optional for tests)
REDIS_HOST=localhost
REDIS_PORT=6379

# File Upload
UPLOAD_MAX_SIZE=10485760
UPLOAD_ALLOWED_TYPES=application/pdf,application/vnd.openxmlformats-officedocument.wordprocessingml.document